## Customize the test machine
general:
  build_dir: stack

machine:
  timezone:
    America/Los_Angeles # Set the timezone

  services:
    - cassandra
    - elasticsearch

# test:
#   pre:
#     - mvn test
